<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">


    <insert id="insertBatch">
        insert into order_detail (order_id, number, dish_id, dish_flavor, setmeal_id, name, image, amount)
        values
        <foreach collection="orderDetails" item="item" separator=",">
            (#{item.orderId}, #{item.number}, #{item.dishId}, #{item.dishFlavor}, #{item.setmealId}, #{item.name}, #{item.image}, #{item.amount})
        </foreach>
    </insert>
</mapper>
